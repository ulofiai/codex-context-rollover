{
  "name": "codex-context-rollover",
  "version": "1.0.0",
  "private": true,
  "description": "Tests and development tooling for the Codex Context Rollover plugin.",
  "type": "module",
  "scripts": {
    "check": "node --check plugins/codex-context-rollover/scripts/post-compact.mjs && npm test",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
